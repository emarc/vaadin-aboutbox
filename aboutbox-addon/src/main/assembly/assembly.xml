<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
              https://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>directory</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <fileSets>
        <!-- MANIFEST.MF filtered so ${project.*} properties are resolved -->
        <fileSet>
            <directory>${project.basedir}/src/main/assembly</directory>
            <outputDirectory>META-INF</outputDirectory>
            <includes>
                <include>MANIFEST.MF</include>
            </includes>
            <filtered>true</filtered>
        </fileSet>
        <!-- Apache License -->
        <fileSet>
            <directory>${project.basedir}/..</directory>
            <outputDirectory>META-INF</outputDirectory>
            <includes>
                <include>LICENSE</include>
            </includes>
        </fileSet>
    </fileSets>

    <files>
        <!-- Flattened POM for consumers -->
        <file>
            <source>${project.basedir}/.flattened-pom.xml</source>
            <outputDirectory>/</outputDirectory>
            <destName>pom.xml</destName>
        </file>
        <!-- Sources and Javadoc jars -->
        <file>
            <source>${project.build.directory}/${project.build.finalName}-sources.jar</source>
            <outputDirectory>/</outputDirectory>
        </file>
        <file>
            <source>${project.build.directory}/${project.build.finalName}-javadoc.jar</source>
            <outputDirectory>/</outputDirectory>
        </file>
    </files>

    <dependencySets>
        <!-- Main add-on JAR; exclude Vaadin — provided by the consuming app -->
        <dependencySet>
            <outputDirectory>/</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <useTransitiveDependencies>false</useTransitiveDependencies>
            <unpack>false</unpack>
            <scope>runtime</scope>
            <excludes>
                <exclude>com.vaadin:*</exclude>
            </excludes>
        </dependencySet>
    </dependencySets>
</assembly>
